import { AnimateValueDirective } from './animate-value.directive';

describe('AnimateValueDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateValueDirective();
    expect(directive).toBeTruthy();
  });
});
